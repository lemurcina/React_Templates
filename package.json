{
  "name": "react_templates",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start:minimal": "lerna run dev:minimal --stream",
    "start:state": "lerna run dev:state --stream",
    "start:web3": "lerna run dev:web3 --stream"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
